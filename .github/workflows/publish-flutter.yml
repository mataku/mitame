name: publish-flutter

on:
  push:
    tags: ["flutter-v[0-9]+.[0-9]+.[0-9]+"]

jobs:
  publish:
    permissions:
      id-token: write
    uses: dart-lang/setup-dart/.github/workflows/publish.yml@6afc89df92d6eb3834022f73cd65adc8cdfcb92d # v1.8.1
    with:
      working-directory: adapters/flutter
