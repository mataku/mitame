name: publish-android

on:
  push:
    tags: ["android-v[0-9]+.[0-9]+.[0-9]+*"]

permissions:
  contents: read

jobs:
  publish:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: adapters/android
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: actions/setup-java@b6effb05e454b25005698d916606bdc6ffcbf961 # v5.7.0
        with:
          distribution: temurin
          java-version: 21
      - run: |
          tag="${GITHUB_REF_NAME#android-v}"
          version="$(sed -n 's/^VERSION_NAME=\(.*\)/\1/p' gradle.properties)"
          echo "tag=$tag android=$version"
          case "$version" in *SNAPSHOT*) echo "refusing to publish a SNAPSHOT" >&2; exit 1 ;; esac
          test "$tag" = "$version"
      - run: ./gradlew publishToMavenLocal -PsignAllPublications=false --no-daemon
      - run: ls -R ~/.m2/repository/io/github/mataku
      - env:
          ORG_GRADLE_PROJECT_mavenCentralUsername: ${{ secrets.ORG_GRADLE_PROJECT_MAVENCENTRALUSERNAME }}
          ORG_GRADLE_PROJECT_mavenCentralPassword: ${{ secrets.ORG_GRADLE_PROJECT_MAVENCENTRALPASSWORD }}
          ORG_GRADLE_PROJECT_signingInMemoryKey: ${{ secrets.ORG_GRADLE_PROJECT_SIGNINGINMEMORYKEY }}
          ORG_GRADLE_PROJECT_signingInMemoryKeyId: ${{ secrets.ORG_GRADLE_PROJECT_SIGNINGINMEMORYKEYID }}
          ORG_GRADLE_PROJECT_signingInMemoryKeyPassword: ${{ secrets.ORG_GRADLE_PROJECT_SIGNINGINMEMORYKEYPASSWORD }}
        run: ./gradlew publishToMavenCentral --no-daemon
      - run: echo "Uploaded ${GITHUB_REF_NAME}. Review the deployment and click Publish at https://central.sonatype.com/publishing" | tee -a "$GITHUB_STEP_SUMMARY"
